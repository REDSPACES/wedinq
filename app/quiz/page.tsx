import GuestQuizContent from "../../components/Pages/GuestQuiz/GuestQuizContent";

/**
 * ゲスト参加画面（スマートフォン用）
 * Server Component
 */
export default function QuizPage() {
  return <GuestQuizContent />;
}
